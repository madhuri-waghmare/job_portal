import React from 'react';

const Home = () => (
    <main>
        <section class="hero">
            <h1>Hello, Welcome to Job Portal</h1>
        </section>
    </main>
);

export default Home;